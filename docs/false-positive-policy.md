# False-positive policy

A false deletion candidate is more costly than an omitted candidate. When evidence conflicts, is missing, or cannot be read, classification must move toward `MANUAL_REVIEW` or `KEEP`, never toward `DELETE_CANDIDATE`.
