# Scan scope

The scanner includes repositories owned by the authenticated personal account with visibility `all`, covering public and private repositories. Collaborator-only and organization-owned repositories are excluded from the current product scope.
