# Manual verification

Before manually deleting any repository, the user should open the repository, inspect its branches, README, releases, issues, pull requests, deployment links, and local relevance. The project's label is evidence assistance, not a substitute for this final check.
