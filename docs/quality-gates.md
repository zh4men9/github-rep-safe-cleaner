# Quality gates

A change is not accepted unless compile, safety scan, unit tests, demo generation, diff review, CI, and Memory Bank consistency are reported separately. Real-account behavior cannot be claimed from fixtures.
