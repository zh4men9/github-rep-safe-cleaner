# Project brief

`github-rep-safe-cleaner` audits all public and private repositories owned by the user and identifies conservative manual cleanup candidates.

The project never mutates inspected repositories. It has no deletion, archiving, visibility, settings, or write capability. The user manually performs any real GitHub action after reviewing every candidate.
