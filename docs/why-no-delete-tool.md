# Why no deletion tool is embedded

Embedding an open-source deletion utility would leave a destructive execution path inside the project. A dry-run default is weaker than having no mutation capability at all. This repository therefore stops at evidence and user decisions.
