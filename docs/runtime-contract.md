# Runtime contract

The runtime may read authenticated account metadata and owned repository metadata through allowlisted GET requests. It may write only local run artifacts under the user-selected output directory. It must not write to GitHub.
