# User command — 2026-07-19

The project must inspect both public and private repositories. It only identifies repositories that may deserve deletion; the user manually performs every actual deletion. The project must remain extremely safe and contain no deletion, archiving, visibility-changing, or inspected-repository write capability. Continue implementation across multiple major versions until real user testing is required.
