# Audit artifacts

Run artifacts are evidence, not source code. Real account artifacts may expose private repository names and therefore remain ignored by Git. Preserve them locally, share only redacted files, and never overwrite an earlier run directory.
