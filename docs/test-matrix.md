# Test matrix

| Layer | Evidence |
|---|---|
| Transport safety | mutation methods and unlisted endpoints are rejected |
| Classification | verified empty, duplicate, fork, public and private cases |
| Reports | all six run artifacts are generated |
| Browser safety | generated page contains no network fetch |
| CI permissions | workflow declares contents: read only |
| Real account | user runs authenticated public/private scan |
