# Credential policy

Credentials are resolved from the process environment or GitHub CLI authentication. They must not be printed, logged, committed, copied into configuration, embedded in artifacts, or preserved in Memory Bank.
