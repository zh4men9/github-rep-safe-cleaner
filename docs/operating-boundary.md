# Operating boundary

The project ends at evidence and local user decisions. The execution boundary begins only when the user separately opens GitHub and performs a manual action. No file, command, exported manifest, or UI control in this repository crosses that boundary.
