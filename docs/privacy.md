# Privacy

The scanner must be run locally by the repository owner. Generated inventories may contain names and metadata for private repositories and are ignored by Git by default.

Do not commit real scan artifacts, exported decisions, tokens, or private repository content. Share only redacted error messages and the minimum evidence needed for debugging.
