# Decision semantics

- `KEEP`: user intends to retain the repository.
- `DELETE_MANUALLY`: user intends to perform a separate manual GitHub deletion after independent verification.
- `REVIEW_LATER`: context is insufficient or the decision is deferred.

These values are local annotations only. They are not commands, approvals for an agent, or inputs to any GitHub mutation API.
